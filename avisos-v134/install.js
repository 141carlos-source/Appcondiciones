(function(){'use strict';
const b=document.getElementById('installBtn');let deferred=null,reloading=false;const standalone=matchMedia('(display-mode: standalone)').matches||navigator.standalone===true;if(standalone)document.body.classList.add('installed');window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferred=e;if(!standalone&&b)b.classList.add('show')});setTimeout(()=>{if(!standalone&&b)b.classList.add('show')},1500);if(b)b.onclick=async()=>{if(deferred){deferred.prompt();try{await deferred.userChoice}catch(e){}deferred=null;b.classList.remove('show');return}alert('PARA INSTALAR SOLTEC AVISOS:\n\nAndroid/Brave/Chrome: menú ⋮ → Instalar aplicación o Añadir a pantalla de inicio.\n\niPhone/iPad: Compartir → Añadir a pantalla de inicio.\n\nPC: usa el icono de instalación de la barra del navegador.')};window.addEventListener('appinstalled',()=>{document.body.classList.add('installed');if(b)b.classList.remove('show')});
async function updateWorker(){if(!('serviceWorker'in navigator))return;try{const reg=await navigator.serviceWorker.register('./sw.js',{updateViaCache:'none'});await reg.update()}catch(e){}}
if('serviceWorker'in navigator){navigator.serviceWorker.addEventListener('controllerchange',()=>{if(reloading)return;reloading=true;location.reload()});window.addEventListener('load',()=>updateWorker());window.addEventListener('pageshow',()=>setTimeout(updateWorker,300));window.addEventListener('online',()=>setTimeout(updateWorker,500));document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')setTimeout(updateWorker,500)})}
})();
